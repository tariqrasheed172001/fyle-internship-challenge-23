import { Component } from '@angular/core';

@Component({
  selector: 'app-no-repos', // Selector used to identify this component in HTML templates
  templateUrl: './no-repos.component.html', // HTML template file for this component
  styleUrls: ['./no-repos.component.scss'], // CSS styles file(s) for this component
})
export class NoReposComponent {
  // This component doesn't have any additional logic or properties.
}
