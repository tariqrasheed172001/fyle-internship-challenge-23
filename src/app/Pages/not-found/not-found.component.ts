import { Component } from '@angular/core';

@Component({
  selector: 'app-not-found', // Selector used to identify this component in HTML templates
  templateUrl: './not-found.component.html', // HTML template file for this component
  styleUrls: ['./not-found.component.scss'], // CSS styles file(s) for this component
})
export class NotFoundComponent {
  // This component doesn't have any additional logic or properties.
}
